<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Index page</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <%- include('header', { title: title, links: links}) %>
    <ul class="messages">
        <% for (let i = 0; i < messages.length; i++) { %>
                <li class="message-board-item">
                    <div>User: <%= messages[i].user %></div>
                    <div><%= messages[i].text %> </div>
                    <div>On: <%= messages[i].added %></div>
                    <a class="open-link" href="/message/<%= messages[i].user %>">Open</a>
                </li>
            <% } %>
    </ul>
</body>
</html>